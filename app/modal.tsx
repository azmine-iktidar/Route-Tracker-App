import { StyleSheet, Text, View } from "react-native";

const modal = () => {
  return (
    <View>
      <Text>modal</Text>
    </View>
  );
};

export default modal;

const styles = StyleSheet.create({});
